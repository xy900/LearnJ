package com.cms.dao;

import org.mybatis.spring.support.SqlSessionDaoSupport;

public class BaseDao<T> extends SqlSessionDaoSupport{
	
	/*<T> selectOne(Integer id) {
		getSqlSession().selectOne(arg0, arg1)
	}*/
}
